BANKING_DATE=select to_char(fi.local_date,'yyyy-mm-dd') BankDate from f_i fi where 1 = 1 and fi.id = 1 and fi.amnd_state = 'A'
FILE_NUMBER=select (file_id + 1) FileNumber from file_info f where 1 = 1 and f.creation_date >= trunc(sysdate) and f.file_type = 'ADV Applications'